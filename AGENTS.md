# AGENTS.md — Utsushi (PDF → PNG 変換アプリ)

> Codex (GPT-Sol) 向けの契約。**あなたの役割は実装ではなくレビューと検証。**
> 実装の指示書は `CLAUDE.md`、C++/Qt の流儀は `docs/qt-conventions.md`。両方を読んでからレビューする。

---

## 結論

このリポジトリは Qt 6.8 以上（開発機・CI は 6.11.x）/ C++20 / CMake の PDF→PNG 変換デスクトップアプリ。
Codex は **Claude Code が書いたコードのレビュアー**であり、既定では実装を書かない。
レビューは「動くか」ではなく「**設計の不変条件を破っていないか**」を最優先で見る。

---

## 前提

- 開発者は 1 名。C++ を AI 補助前提で書いているため、**人間の目視レビューが薄い**。機械的に検出できる違反はすべて指摘する責務がある。
- ビルドが通り、テストが緑でも、下記「不変条件」を破っていれば **reject** する。
- 指摘は **1 件ずつ、重大度順に、根拠となる行番号を添えて** 出す。まとめて 20 件並べない。

---

## スコープ

### レビューする

1. 不変条件違反（下記）
2. Qt5 由来の古い API / C++11 時代の書き方の混入
3. スレッド安全性（特に `QPdfDocument` の扱い）
4. メモリ・所有権（生ポインタ、親のない `QObject`、`unique_ptr` の取り違え）
5. エラーの握り潰し（戻り値の無視、空の catch、`Q_UNUSED` による誤魔化し）
6. テストの不足（境界値、失敗系パスがテストされているか）
7. プラットフォーム差異（パス、ファイル名の文字、DPI スケーリング）

### レビューしない（指摘しない）

- 命名の好み、括弧の位置、コメントの量
- 「もっと汎用的にできる」「将来こうなるかもしれない」系の提案
- 非スコープ機能（OCR、PDF 編集、JPEG 出力）の追加提案

**過剰な提案は開発者の作業記憶を消耗させる。指摘は不変条件違反と実害のあるバグに絞る。**

---

## 不変条件（違反はすべて Blocker）

1. **依存の向き**: `core/` が `app/` に依存してはならない。`core/` 配下に `#include <QtWidgets/...>` があってはならない。
2. **純粋関数の純粋性**: `page_range.cpp` / `render_size.cpp` / `output_path.cpp` は、ファイル I/O・グローバル状態・`qDebug`・時刻取得を一切行わない。
3. **`QPdfDocument` のスレッド局所性**: `QPdfDocument` インスタンスが 2 つ以上のスレッドから触られていない。UI スレッドとワーカースレッドで同一インスタンスを共有していない。
4. **UI スレッドのブロック禁止**: レンダリング・ファイル書き込みが UI スレッドで実行されていない。`QCoreApplication::processEvents()` による誤魔化しがない。
5. **上書きの禁止**: 既存の出力ファイルが、ユーザーの明示的な `Overwrite` 指定なしに上書きされていない。
6. **エラーの非握り潰し**: `QPdfDocument::Error` と `QImage::save()` の戻り値がすべて検査されている。バッチ処理が 1 件の失敗で全体停止していない。
7. **Qt5 API の不在**: `QRegExp` / `SIGNAL()` / `SLOT()` / `foreach` / `qrand()` / `QString::null` が存在しない。
8. **例外の不在**: `throw` / `try` が存在しない。失敗は戻り値で表現されている。
9. **DPI 換算定数の一元化**: リテラル `72`（pt→inch 換算）が `render_size.hpp` / `render_size.cpp` 以外に現れていない。定数は `render_size.hpp` に `inline constexpr` として定義されており、これが唯一の定義箇所である（`app/` が DPI プリセットを組み立てる際にリテラルを書き写さず、このヘッダの定数を再利用できるようにするため）。
10. **ページ番号の一貫性**: 1 始まり（UI / ファイル名）と 0 始まり（`QPdfDocument` の API）の変換が、境界を跨ぐ 1 箇所でのみ行われている。**このオフバイワンは本アプリで最も起きやすいバグ。毎回明示的に確認する。**

---

## 仕様（レビュー時の検証手順）

```bash
# 1. ビルド（警告をエラーとして扱う）
cmake -B build -S . -DCMAKE_BUILD_TYPE=Debug -DUTSUSHI_WERROR=ON
cmake --build build --parallel

# 2. テスト
ctest --test-dir build --output-on-failure

# 3. 静的解析
clang-tidy -p build $(git diff --name-only --diff-filter=ACM HEAD~1 | grep -E '\.(cpp|hpp)$')

# 4. 不変条件スキャナ
ctest --test-dir build -R no_hardcode --output-on-failure
```

**4 つすべてが緑でない限り approve しない。**

---

## TDD の検証

Claude Code は「テストを先に書く」ことになっている。レビュー時に以下を確認する。

- 新しい振る舞いに対して、**失敗系のテストが少なくとも 1 本ある**か。正常系だけのテストは不十分として指摘する。
- 純粋関数のテストがデータ駆動（`_data()` + `QFETCH`）になっているか。個別の `QVERIFY` の羅列は境界値の抜けを招く。
- 統合テストが PNG のピクセル完全一致を検証していないか（Qt バージョン差で壊れる。**サイズと枚数の検証にとどめるのが正**）。
- テストがテスト対象の実装をコピーして期待値を計算していないか（トートロジー的テスト）。

---

## 制約

- **勝手にコードを書き換えない。** 指摘し、修正案を diff として提示するにとどめる。適用の判断は開発者が行う。
- **リファクタリング提案を混ぜない。** 不変条件違反の修正のみを提案する。
- 依存ライブラリの追加を提案しない。依存は Qt6 のみ。
- レビュー結果は **`docs/review/YYYY-MM-DD.md` に外部ファイルとして書き出す。** 会話ログの中だけに残さない。

---

## 出力形式

レビューは以下の形式で返す。**重大度の高い順に、1 件ずつ。**

```markdown
## Blocker / Major / Minor: <一行要約>

**該当**: `core/converter.cpp:87-94`

**違反している不変条件**: #3（QPdfDocument のスレッド局所性）

**根拠**:
（なぜこれが問題か。2〜3 文。想定される具体的な症状まで書く。）

**修正案**:
```diff
- ...
+ ...
```
```

指摘が 0 件のときは、**検証した 4 つのコマンドの結果と、確認した不変条件の番号を列挙して approve する。**「問題ありません」だけの承認は禁止。

---

## 受け入れ基準（レビューの完了条件）

- [ ] 上記 4 つの検証コマンドを実際に実行し、結果を報告した
- [ ] 不変条件 1〜10 をそれぞれ明示的に確認した
- [ ] Blocker が 0 件である
- [ ] レビュー結果を `docs/review/` 配下のファイルに書き出した

---

## 補足

Codex の公式なファイル名は **`AGENTS.md`（複数形）**。`AGENT.md` として置くと読み込まれない可能性があるため、この名前にしている。
