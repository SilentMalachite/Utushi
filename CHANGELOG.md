# 変更履歴

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

---

## [0.1.0] - 2026-08-04

最初のリリース。受け入れ基準の達成状況は [CLAUDE.md の受け入れ基準](CLAUDE.md#受け入れ基準v010)、残っている制限は [docs/known-issues.md](docs/known-issues.md) を参照してください。

### 追加

- PDF を PNG 連番画像へ変換する機能。ラスタライズは Qt 標準の QtPdf（`QPdfDocument`）のみを使用し、外部プロセスに依存しない
- 複数 PDF のバッチ変換。1 件の失敗で全体を止めず、最後に理由付きのサマリを表示する
- ページ範囲指定（空欄で全ページ、`1-5,8,11-` 形式）
- 出力解像度の指定（72 / 150 / 300 / 600 のプリセットと任意値。1 辺 20000px を上限とする）
- 出力ファイル名の規則 `{stem}_p{ゼロ埋めページ番号}.png`。ファイル名に使えない文字のサニタイズと NFC 正規化を含む
- 既存ファイルの扱いを選択できる（スキップ（既定）/ 上書き / 別名で保存）。既定では既存ファイルを黙って上書きしない
- 進捗表示（プログレスバーとテキストの両方）とキャンセル。キャンセル後も途中まで書き出した PNG は残す
- ワーカースレッドでの変換。`QPdfDocument` はワーカー局所に持ち、UI スレッドと共有しない
- `ヘルプ > ライセンス` のライセンス告知ダイアログと、配布物への `LICENSE` 同梱
- GitHub Actions CI（macOS arm64 / Windows x64）。Release + 警告エラー化でのビルド、`ctest`、`macdeployqt` / `windeployqt`、配布物の静的検証、Qt を環境から取り除いた状態での起動確認まで
- 規約スキャナ `tst_no_hardcode`（不変条件テスト）と `.clang-tidy` のチェックセット
- ドキュメント: `README.md`、`docs/build.md`、`docs/usage.md`、`docs/known-issues.md`、`CHANGELOG.md`

### 変更

- Windows の CI を Qt 6.10.\* に固定した。上流が Windows 向け 6.11.x のバイナリを公開していないため（[復帰条件](docs/known-issues.md#windows-向け-qt-611x-のバイナリが上流に存在しない)）
- 本アプリのライセンスを GPLv3 と定めた（当初の想定は Apache-2.0 だった）

### 削除

- パスワード保護・暗号化 PDF への対応。`QPdfWriter` が暗号化 PDF を生成できずテスト用フィクスチャを作れないため、実装ごと落とした。渡された場合は「パスワード保護・暗号化された PDF には対応していません」という理由で失敗として記録し、バッチは次のファイルへ進む（[理由](docs/known-issues.md#パスワード保護や暗号化された-pdf-は開けない)）

### 修正（初回リリース前の開発中に発見・修正したもの）

- **出力 PNG の背景が透明だった。** `QPdfDocument::render()` は紙の色を塗らないため、保存された PNG は「白い紙に黒い文字」ではなく「透明な背景に黒い文字」になっており、ダークモードのビューアで開くと文字が背景に溶けて読めなかった。描画内容の下に白を敷いて紙を再現するよう修正
- 変換前の検査で弾かれた失敗が、最終サマリから黙って消えていた
- ウィンドウを閉じるときにバッチ全体の完了を待ってフリーズしていた（150 ページのジョブで実測 36.5 秒 → 233ms）
- 一度キャンセルすると、2 回目以降の変換が「開始した瞬間に完了」して何も変換されなかった
- 同じ出力ファイル名になる入力が同一バッチ内にあると、黙って衝突していた（サニタイズ後に同名になる場合、大文字小文字だけが違う場合を含む）
- スキップ / 別名保存が「存在を確認してから書く」方式で、確認と書き込みの間に別プロセスが作ったファイルを壊し得た。保存先を排他的に確保してから書く方式に変更
- PNG の保存に 1 件失敗しただけでバッチ全体を中止していた
- 失敗したページが進捗に数えられず、プログレスバーが 100% に達しないまま「完了」と表示されていた
- すべてのファイルが変換前の検査で失敗しても「完了」と表示していた
- 変換前の検査が、具体的なエラー理由ではなく一律の文言を表示していた
- 実行ファイルが `.app` バンドルとしてビルドされておらず、`macdeployqt` を適用できなかった
- 非有限の DPI / ページサイズ（NaN・無限大）を弾いていなかった
- Windows の配布物にテストバイナリが混入していた
- 出力ファイル名の桁数計算に縮小変換が含まれていた
